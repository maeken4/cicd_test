name: Hello
on: push
jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - run: |
                echo "hello, world"
                tree
                invalid-command
            - uses: actions/checkout@v4
              with:
                ref: main
